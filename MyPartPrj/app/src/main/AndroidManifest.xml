<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.onest.mypartprj">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WrongTiListActivity" android:label="错题列表"/>
        <activity android:name=".WrongDetailActivity" android:label="错题详情"/>
        <activity android:name=".VIPYueBaoActivity" android:label="会员月报"/>
        <activity android:name=".ExerciseActivity" android:label="练习"/>
        <activity android:name=".DfActivity" android:label="自定义错题"/>
        <activity android:name=".WrongGridActivity" android:label="自定义错题"/>
    </application>

</manifest>